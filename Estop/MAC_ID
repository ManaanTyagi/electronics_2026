#include <WiFi.h>

void setup() {
  Serial.begin(115200);
  delay(1000);

  WiFi.mode(WIFI_MODE_STA);  // important
  WiFi.begin();              // init WiFi stack

  delay(500);                // give time

  Serial.println(WiFi.macAddress());
}

void loop() {}